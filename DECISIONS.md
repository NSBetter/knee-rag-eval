# 项目决策日志

本文件记录项目中的关键设计选择、原因、替代方案和后续验证结果。

---

## D-001：将项目范围限定为成人膝痛与膝骨关节炎

**日期：** 2026-07-30

**决策：**

第一版仅覆盖 18 岁及以上普通用户和家属的膝痛及膝骨关节炎咨询。

**原因：**

1. 与项目负责人的骨科和膝骨关节炎背景一致；
2. 能够对医学资料、参考答案和风险标签进行可靠核验；
3. 场景足够覆盖知识问答、治疗、药物、风险分诊和拒答；
4. 避免扩大为无法在一周内完成和评测的全科医疗系统。

**未选择的方案：**

- 全科医疗问答；
- 全部骨科疾病；
- 仅做膝骨关节炎知识问答。

**风险：**

范围仍可能过大，后续需要根据文献和时间进一步压缩。

---

## D-002：第一版不开发 Streamlit 界面

**日期：** 2026-07-30

**决策：**

第一版不制作聊天网页，将时间优先投入离线 RAG 和评测管线。

**原因：**

1. 求职岗位重点是训练数据、模型评测和错误归因；
2. 页面本身不能证明系统医学质量；
3. 一周项目周期有限；
4. 命令行和结果报告已经足以展示核心工作流。

**后续选择：**

如核心评测管线提前完成，可增加简易展示界面。

---

## D-003：不将原始指南 PDF 上传公开 GitHub

**日期：** 2026-07-30

**决策：**

原始指南仅保存在本地 `data/corpus/`，公开仓库不包含指南全文。

**原因：**

1. 阅读和使用权限不等同于公开再分发权限；
2. 避免潜在版权问题；
3. 仓库通过资料登记表和获取说明保证可追溯性。

---

## D-004：第一版使用云端模型，不要求完全本地运行

**日期：** 2026-07-30

**决策：**

生成模型和 Embedding 可采用云端 API；文档、评测集和实验结果保存在本地。

**原因：**

1. 当前设备为 M2 芯片、16 GB 内存；
2. 本地大模型部署会增加配置成本；
3. 项目核心是理解 RAG 与评测工作流，而不是本地推理性能；
4. 云端模型有利于在一周内完成稳定基线。

**风险：**

- API 成本；
- 网络依赖；
- 模型版本变化；
- 数据上传边界。

**控制措施：**

- 不上传真实患者数据；
- 保存模型名称、Prompt、时间和参数；
- 设置调用数量及成本上限。
---

## D-005：保留自动PDF文本提取，改用版面感知的文本块重排

**日期：** 2026-07-31

**决策：**

继续使用原始PDF自动构建知识库，不改为纯人工整理证据库。

中文双栏指南使用带页面坐标的文本块进行左右栏重排；英文指南使用单栏文本块提取，并自动清除重复页眉、页脚和行号。

表格内容后续单独识别和结构化，不依赖普通正文提取恢复表格布局。

**原因：**

1. 五份PDF均包含可直接提取的文字层，不需要OCR；
2. 当前主要问题是版面阅读顺序，而不是文字无法识别；
3. 自动处理流程更能展示RAG数据入库、质量控制和错误分析能力；
4. 保留页码和坐标有助于后续引用追溯。

**已发现的问题：**

- SRC001至SRC004存在双栏正文交叉；
- 五份文档均存在不同程度的页眉、页脚或表格提取问题；
- SRC005每行前存在预印本行号；
- SRC001存在少量异常字符，需要继续定位具体类型。

**风险：**

- 复杂表格仍可能无法通过普通文本块提取恢复；
- 双栏中的跨栏标题、脚注和图注可能需要额外规则；
- 不同指南版式不同，通用规则可能需要按文档配置。

---

## D-006：第一版不将自动表格提取结果纳入RAG知识库

**日期：** 2026-07-31

**决策：**

第一版继续使用自动化PDF正文提取，但不将当前自动识别的表格内容纳入知识库。

对于人工确认存在复杂表格或图表穿插的页面，通过页级配置从正文切块流程中排除。自动表格检测和Markdown输出仅作为数据质量审计结果保留。

**实验结果：**

比较了以下三种表格处理方案：

1. PyMuPDF默认 `find_tables()`；
2. PyMuPDF `strategy="text"`；
3. PyMuPDF4LLM与PyMuPDF Layout。

三种方法均能在部分页面报告表格边界，但人工检查发现：

- 默认方法和text策略生成的表格内容为空；
- Layout方法将部分普通正文误识别为表格；
- 自动检测数量不能代表表格内容已经正确恢复；
- 当前输出不适合直接进入医学RAG知识库。

**原因：**

1. 错误表格结构可能破坏推荐意见、适用条件和风险信息；
2. 一周MVP应优先完成正文检索、QA评测和错误分析；
3. 继续调试复杂表格提取的边际收益较低；
4. 项目已经保留自动提取实验和失败分析，可体现数据质量治理过程。

**后续方案：**

- 第一版仅索引经过自动清洗并通过抽检的正文；
- 对表格密集页面设置排除或人工复核标签；
- 如评测题确实依赖关键表格，可在后续版本建立人工核验的结构化表格摘要；
- 不将当前自动表格Markdown视为可信医学证据。
---

## D-007：放弃通用正则驱动的章节切块，改用文档边界配置

**日期：** 2026-07-31

**决策：**

不将当前章节感知切块结果作为正式知识库。保留自动PDF提取，但下一版增加文档级正文起止范围、参考文献边界和特殊页面配置，并仅使用高精度章节标题规则。

**实验结果：**

章节感知切块共生成319个文本块。25个抽检块中：

- 2个可直接用于检索；
- 6个部分可用；
- 17个不可用；
- 6个从半句话开始；
- 4个在半句话结束；
- 11个存在不同程度的作者信息噪声；
- 5个仍存在英文行号；
- 13个上下文不完整。

与基线相比，可直接用于检索的比例没有改善。

**主要错误原因：**

1. DOI和参考文献条目被误识别为章节标题；
2. 参考文献标题在PDF提取后并不总是独立文本块；
3. 错误章节标题被传播到后续页面；
4. 作者、单位和附录信息未被完整排除；
5. 通用标题正则无法适配五份不同版式的指南；
6. 英文行号需要在版面坐标层处理。

**后续方案：**

建立文档级正文边界配置，在自动提取后先可靠排除前置作者信息、参考文献和尾部非医学内容，再进行保守的章节与句子切块。

---

## D-008：第一版检索库暂不纳入SRC005

**日期：** 2026-07-31

**决策：**

OARSI英文指南继续保留在资料登记、PDF审计和解析实验中，但不进入MVP第一版向量索引。

**原因：**

1. 英文文档仍存在行号残留和异常段落边界；
2. 67个切块中有65个被错误标记为跨页，提示合并规则过于激进；
3. 人工抽检的7个SRC005切块均只能部分用于检索；
4. 当前四份中文指南已经能够覆盖项目第一版的主要问答范围；
5. 一周MVP应优先完成检索、QA评测和错误分析，而不是继续投入英文预印本清洗。

**说明：**

本决策不代表OARSI指南医学价值较低，也不代表系统不支持英文文档，而是基于当前PDF版式和项目周期作出的工程取舍。

**后续版本：**

可改用无行号的正式出版版本、HTML正文或更可靠的版面解析方法后重新纳入。

---

## D-009：MVP正式索引仅纳入SRC001与SRC003

**日期：** 2026-08-01

**决策：**

MVP正式检索库仅纳入：

1. SRC001《中国骨关节炎诊疗指南（2024版）》；
2. SRC003《中国膝骨关节炎临床药物治疗专家共识》。

SRC002、SRC004和SRC005继续保留在资料登记、PDF审计和解析实验中，但暂不进入`gold_v1_3`索引。

**原因：**

1. SRC001提供定义、临床表现、诊断和综合治疗框架；
2. SRC003提供药物选择、适用条件、风险和特殊人群信息；
3. 两份资料内容互补，足以覆盖MVP问答和安全评测；
4. 不同指南知识组织形式差异明显，强行统一切块会降低语料质量；
5. 优先完成完整的检索与评测闭环，比扩大文献数量更有工程价值。

**说明：**

这是基于解析质量、内容覆盖和项目周期作出的工程范围控制，不代表其他指南医学价值较低。

---

## D-010：采用人工结构标注与文档专属切块策略

**日期：** 2026-08-01

**决策：**

不再依赖通用正则自动猜测全部标题。由人工提供并核验`document_structure_map.csv`，程序使用：

- `marker_text`；
- `marker_occurrence`；
- `parent_id`；
- `node_type`；
- `include_status`；
- `extraction_mode`

共同定位知识结构。

SRC001采用层级章节切块，SRC003采用“临床问题—一个或多个推荐意见”切块。

**原因：**

1. SRC001以章节、次级标题和治疗层级组织；
2. SRC003以临床问题和推荐意见组织；
3. 同一短标题可能在一页出现多次，仅靠字符串唯一性无法稳定定位；
4. 医学知识块需要保留完整标题路径、问题语境和推荐条件；
5. 人工标注结构、程序自动提取正文属于可审计的human-in-the-loop数据治理。

**验证要求：**

结构表必须通过自动验证，包括：

- 节点ID和顺序唯一；
- 父子层级正确；
- marker在指定页可定位；
- `marker_occurrence`不越界；
- 节点位于已验证正文边界内；
- 提取模式与纳入状态一致。

---

## D-011：关键表格采用截图转录、人工核验和结构化入库

**日期：** 2026-08-01

**决策：**

SRC001中对MVP评测重要、但自动表格提取不可靠的内容采用：

```text
表格截图
→ 结构化转录草稿
→ 对照PDF人工核验
→ CSV正式入库
```

首批人工核验内容包括：

- 膝骨关节炎常见症状；
- 膝骨关节炎常见体征；
- 膝骨关节炎诊断标准；
- 影像学检查说明。

**数据源约定：**

- `manual_table_evidence.csv`是唯一机器可读真源；
- XLSX仅作为本地人工审核底稿；
- 公开GitHub仅保存不含指南正文的审核摘要、哈希和验证状态；
- 所有正式表格证据必须标记为`human_verified`。

**原因：**

1. 表格包含症状、体征、诊断组合规则等关键医学信息；
2. 完全排除会削弱病例问答和诊断边界评测；
3. 错误自动表格比缺失表格风险更高；
4. 人工核验能够明确区分原文转录、模型归纳和工程加工。

---

## D-012：Gold Corpus经过多轮错误驱动优化后冻结为gold_v1_3

**日期：** 2026-08-01

**决策：**

最终MVP语料冻结为：

```text
corpus_version = gold_v1_3
```

在语料人工抽检通过前不建立Embedding和向量索引。

**版本演进：**

### v1.1

引入结构节点驱动切块，并跳过只有标题、没有独立正文的父节点。父标题仍保留在子节点的`title_path`中。

### v1.2

修复：

- 文档最后节点越过正文尾标记；
- 作者名单和参考文献污染；
- SRC003概述正文锚点被误删；
- 部分SRC001表格穿插；
- 推荐续块只携带缩略标题。

v1.2人工抽检43个块，其中34个可直接用于检索、9个部分可用、0个不可用。剩余问题集中在3个超长推荐组。

### v1.3

针对长推荐进行收尾修复：

- 不优先在逗号和顿号处切分；
- 允许为保持完整医学句子适度超过目标长度；
- 续块重复完整问题和完整推荐核心；
- 对问题2、问题4和问题8使用经过核验的完整推荐上下文；
- 抽检同时覆盖推荐首块和续块。

**冻结标准：**

- 无不可用知识块；
- 问题与推荐核心完整；
- 无作者名单和参考文献噪声；
- 无由切块器造成的医学条件截断；
- 标题路径和来源页码可追溯；
- 剩余轻微PDF语序问题不影响医学含义。

---

## D-013：公开仓库保存工程证据，本地保存受限语料

**日期：** 2026-08-01

**决策：**

GitHub公开仓库保存：

- 源代码；
- 配置文件；
- 项目章程和决策日志；
- 不含指南正文的审计与统计结果；
- 失败实验及其公开摘要；
- 可复现运行说明。

本地保存且不公开：

- 原始指南PDF；
- 自动提取的完整正文；
- 页级JSONL；
- 人工表格原文CSV和审核XLSX；
- 完整Gold Corpus CSV/JSONL；
- 含指南正文的人工抽检表；
- API密钥和模型调用缓存。

**原因：**

1. 避免公开再分发指南全文；
2. 防止公开仓库包含受版权保护的长文本；
3. 通过脚本、配置、哈希和统计摘要仍可证明流程可复现；
4. 明确区分工程资产、公开证据和本地受限数据。

**版本管理要求：**

- 完成一个可独立验证的步骤后提交一次Git commit；
- commit只代表写入本地Git历史；
- 只有执行`git push`后才算上传到GitHub；
- 正式进入下一阶段前运行项目资产审计，确认必要公开文件已跟踪、已提交且本地分支已推送。

## RET-006 query boundary revision

原问题：
“不想吃药的话，有哪些其他治疗方法呢？”

问题范围过宽，导致检索结果混入药物治疗、手术治疗等多个分支。

修改为：
“不想吃药，膝骨关节炎的基础治疗包括哪些措施？”

并重新审核证据范围。

最终定义：
- Gold:
  - SRC003-QR-022-023-C01
  - SRC003-QR-022-023-C02
- Source:
  - SRC003
- Evidence scope:
  - multi_chunk

该修改用于提高Benchmark标签稳定性，而非优化检索性能。

## D-014：冻结Retrieval Pilot Benchmark

**决策：**

冻结当前12条Retrieval Pilot Benchmark，版本标记为
`retrieval_eval_v1_pilot_frozen`。

Pilot包含：

- 11条可回答问题；
- 1条不可回答问题；
- 8条单证据问题；
- 3条多证据问题；
- 1条无Gold问题；
- 1条跨来源问题。

RET-006在检索错误分析后由宽泛的：

> 不想吃药的话，有哪些其他治疗方法呢？

收窄为：

> 不想吃药，膝骨关节炎的基础治疗包括哪些措施？

RET-006最终定义为跨来源、多Chunk问题。

Gold证据为：

- `SRC003-QR-022-023-C01`
- `SRC003-QR-022-023-C02`
- `SRC001-SEC-018-C01`

来源范围为：

- `SRC001`
- `SRC003`

冻结后不得根据BM25、Dense或Hybrid排名表现继续修改Query、
Gold或Support。若发现事实性标注错误，应创建新的Benchmark版本，
并重新运行全部检索基线。

## D-015：Dense作为MVP默认检索器

**实验结果：**

在冻结的Retrieval Pilot Benchmark上：

| Retriever | Hit@1 | Hit@3 | Hit@5 | Recall@5 | MRR@10 |
|---|---:|---:|---:|---:|---:|
| BM25 | 0.7273 | 0.8182 | 0.8182 | 0.7424 | 0.7857 |
| Qwen3 Dense | 0.8182 | 1.0000 | 1.0000 | 0.9697 | 0.8939 |
| BM25 + Dense RRF | 0.7273 | 0.8182 | 0.9091 | 0.8788 | 0.8061 |

**决策：**

MVP阶段采用`Qwen3-Embedding-0.6B` Dense Retrieval作为默认检索器。

BM25保留为可复现的词法检索基线；等权RRF保留为Hybrid消融实验，
但不作为默认方案。

**原因：**

RRF在RET-002中将Dense排名第1的Gold降至第6，并在RET-004中将
Gold由第3降至第5，说明BM25噪声会通过等权排名融合削弱Dense结果。

RET-006的第三个Gold没有进入BM25和Dense的Top 10，因此当前RRF无法
解决该证据缺失问题。

后续只有在扩展Benchmark完成后，才重新评估加权融合、候选深度扩展
或其他Hybrid策略，避免在12条开发Pilot上过拟合。

cat >> DECISIONS.md <<'EOF'

## D-016：受限Recommendation Override与公开配置分离

**决策：**

`gold_v1_3`中的人工核验Recommendation Override采用公开规则与
本地受限文本分离的方式管理。

公开仓库中的`configs/gold_corpus_cleanup_v1_3.json`仅保存：

- 清理和构建规则；
- 必需的Recommendation Override节点ID。

具体的人工核验指南文本保存在：

`data/processed/private/gold_corpus_recommendation_overrides_v1_3.json`

该路径由Git忽略，不进入公开仓库。

`build_gold_corpus.py`在构建时加载并合并本地Override；若必需的本地
文件或对应节点缺失，则立即终止构建，避免生成内容不完整但版本号仍为
`gold_v1_3`的Corpus。

**原因：**

该设计保持Gold Corpus构建逻辑可审计，同时避免在公开配置中直接保存
受限来源文本。迁移前后Gold Corpus及相关构建产物的SHA-256保持一致，
因此该调整不改变已冻结Retrieval实验所使用的语料内容。
EOF

## D-017：冻结Generation + Automated Evaluation MVP v1

**决策：**

在12条冻结Retrieval Pilot Benchmark上跑通Generation + Automated
Evaluation完整闭环后，将当前实现冻结为MVP v1。

当前v1流水线为：

1. 使用Qwen3 Dense Retrieval的Top-5结果；
2. 根据`retrieved_chunk_id`回查Gold Corpus正文；
3. 构建不包含Gold/Support标签的Generation输入；
4. 使用`generation_prompt_v1`和DeepSeek V4 Flash生成回答；
5. 使用确定性规则检查生成成功、非空回答、Evidence数量和
   unanswerable基本行为；
6. 使用DeepSeek V4 Pro作为LLM-as-a-Judge，对Relevance、
   Groundedness、Completeness、Unsupported Claim和
   Unanswerable Behavior进行结构化评测；
7. 自动筛选边界案例进入人工医学复核；
8. 使用人工复核结果校准LLM Judge。

**Pilot结果：**

- Generation成功：12 / 12；
- Deterministic Rules通过：12 / 12；
- LLM Judge成功：12 / 12；
- Judge判定pass：12 / 12；
- 检测到Unsupported Claim：1条；
- 自动进入人工复核：2条（RET-002、RET-012）；
- 首批Human Calibration：2条；
- Judge-Human逐字段一致：10 / 10。

**关键边界：**

Generation输入不得包含`gold_chunk_ids`、`supporting_chunk_ids`、
`is_gold`或`is_support`等评测标签，避免Generation阶段发生
Gold Label Leakage。

LLM Judge不是最终Gold标准。人工医学复核结果作为Judge Calibration
参照。

当前仅有2条Human Calibration样本，因此10 / 10字段一致只能证明
Judge在这两个边界案例上与人工一致，不能证明Judge已具备充分的
泛化可靠性。

**后续策略：**

不继续针对当前12条Pilot优化Judge或Generation Prompt。

当前12条Pilot保留为Development / Smoke Test集合。下一阶段优先扩展
Benchmark，在新的问题上运行不修改的Evaluator v1，根据新的错误案例
和Judge-Human分歧再决定是否开发Evaluator v2。

该策略用于降低评测器对当前小规模Pilot的过拟合风险。
