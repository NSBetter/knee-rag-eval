# 项目决策日志

本文件记录项目中的关键设计选择、原因、替代方案和后续验证结果。

---

## D-001：将项目范围限定为成人膝痛与膝骨关节炎

**日期：** 2026-07-30

**决策：**

第一版仅覆盖 18 岁及以上普通用户和家属的膝痛及膝骨关节炎咨询。

**原因：**

1. 与项目负责人的骨科和膝骨关节炎背景一致；
2. 能够对医学资料、参考答案和风险标签进行可靠核验；
3. 场景足够覆盖知识问答、治疗、药物、风险分诊和拒答；
4. 避免扩大为无法在一周内完成和评测的全科医疗系统。

**未选择的方案：**

- 全科医疗问答；
- 全部骨科疾病；
- 仅做膝骨关节炎知识问答。

**风险：**

范围仍可能过大，后续需要根据文献和时间进一步压缩。

---

## D-002：第一版不开发 Streamlit 界面

**日期：** 2026-07-30

**决策：**

第一版不制作聊天网页，将时间优先投入离线 RAG 和评测管线。

**原因：**

1. 求职岗位重点是训练数据、模型评测和错误归因；
2. 页面本身不能证明系统医学质量；
3. 一周项目周期有限；
4. 命令行和结果报告已经足以展示核心工作流。

**后续选择：**

如核心评测管线提前完成，可增加简易展示界面。

---

## D-003：不将原始指南 PDF 上传公开 GitHub

**日期：** 2026-07-30

**决策：**

原始指南仅保存在本地 `data/corpus/`，公开仓库不包含指南全文。

**原因：**

1. 阅读和使用权限不等同于公开再分发权限；
2. 避免潜在版权问题；
3. 仓库通过资料登记表和获取说明保证可追溯性。

---

## D-004：第一版使用云端模型，不要求完全本地运行

**日期：** 2026-07-30

**决策：**

生成模型和 Embedding 可采用云端 API；文档、评测集和实验结果保存在本地。

**原因：**

1. 当前设备为 M2 芯片、16 GB 内存；
2. 本地大模型部署会增加配置成本；
3. 项目核心是理解 RAG 与评测工作流，而不是本地推理性能；
4. 云端模型有利于在一周内完成稳定基线。

**风险：**

- API 成本；
- 网络依赖；
- 模型版本变化；
- 数据上传边界。

**控制措施：**

- 不上传真实患者数据；
- 保存模型名称、Prompt、时间和参数；
- 设置调用数量及成本上限。
---

## D-005：保留自动PDF文本提取，改用版面感知的文本块重排

**日期：** 2026-07-31

**决策：**

继续使用原始PDF自动构建知识库，不改为纯人工整理证据库。

中文双栏指南使用带页面坐标的文本块进行左右栏重排；英文指南使用单栏文本块提取，并自动清除重复页眉、页脚和行号。

表格内容后续单独识别和结构化，不依赖普通正文提取恢复表格布局。

**原因：**

1. 五份PDF均包含可直接提取的文字层，不需要OCR；
2. 当前主要问题是版面阅读顺序，而不是文字无法识别；
3. 自动处理流程更能展示RAG数据入库、质量控制和错误分析能力；
4. 保留页码和坐标有助于后续引用追溯。

**已发现的问题：**

- SRC001至SRC004存在双栏正文交叉；
- 五份文档均存在不同程度的页眉、页脚或表格提取问题；
- SRC005每行前存在预印本行号；
- SRC001存在少量异常字符，需要继续定位具体类型。

**风险：**

- 复杂表格仍可能无法通过普通文本块提取恢复；
- 双栏中的跨栏标题、脚注和图注可能需要额外规则；
- 不同指南版式不同，通用规则可能需要按文档配置。

---

## D-006：第一版不将自动表格提取结果纳入RAG知识库

**日期：** 2026-07-31

**决策：**

第一版继续使用自动化PDF正文提取，但不将当前自动识别的表格内容纳入知识库。

对于人工确认存在复杂表格或图表穿插的页面，通过页级配置从正文切块流程中排除。自动表格检测和Markdown输出仅作为数据质量审计结果保留。

**实验结果：**

比较了以下三种表格处理方案：

1. PyMuPDF默认 `find_tables()`；
2. PyMuPDF `strategy="text"`；
3. PyMuPDF4LLM与PyMuPDF Layout。

三种方法均能在部分页面报告表格边界，但人工检查发现：

- 默认方法和text策略生成的表格内容为空；
- Layout方法将部分普通正文误识别为表格；
- 自动检测数量不能代表表格内容已经正确恢复；
- 当前输出不适合直接进入医学RAG知识库。

**原因：**

1. 错误表格结构可能破坏推荐意见、适用条件和风险信息；
2. 一周MVP应优先完成正文检索、QA评测和错误分析；
3. 继续调试复杂表格提取的边际收益较低；
4. 项目已经保留自动提取实验和失败分析，可体现数据质量治理过程。

**后续方案：**

- 第一版仅索引经过自动清洗并通过抽检的正文；
- 对表格密集页面设置排除或人工复核标签；
- 如评测题确实依赖关键表格，可在后续版本建立人工核验的结构化表格摘要；
- 不将当前自动表格Markdown视为可信医学证据。